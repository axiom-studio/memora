// Package main is the Memora CLI entrypoint.
//
// The full CLI implementation lands in feature F9 (memora-cli).
// This stub exists so `go build ./...` and `go vet ./...` succeed
// on the freshly-initialized module.
package main

import "fmt"

func main() {
	fmt.Println("memora-cli: not yet implemented (see PRD #295 / feature F9)")
}
