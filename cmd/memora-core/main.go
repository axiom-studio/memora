// Package main is the Memora Core server entrypoint.
//
// The full server implementation lands in feature F5 (HTTP API).
// This stub exists so `go build ./...` and `go vet ./...` succeed
// on the freshly-initialized module.
package main

import "fmt"

func main() {
	fmt.Println("memora-core: not yet implemented (see PRD #297 / feature F5)")
}
